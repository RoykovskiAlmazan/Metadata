<Window x:Class="Conversor.EditarComandoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar comando"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!-- Categoría -->
        <TextBlock Text="Categoría:" Grid.Row="0" Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"/>

        <ComboBox Grid.Row="0"
                  Grid.Column="1"
                  Margin="0,0,0,8"
                  SelectedValuePath="Content"
                  SelectedValue="{Binding Categoria, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBoxItem Content="start"/>
            <ComboBoxItem Content="velocidad"/>
            <ComboBoxItem Content="inclinacion"/>
        </ComboBox>

        <!-- Valor -->
        <TextBlock Text="Valor:" Grid.Row="1" Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"/>

        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Margin="0,0,0,8"
                 Text="{Binding Valor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- t_s -->
        <TextBlock Text="t_s:" Grid.Row="2" Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"/>

        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 Margin="0,0,0,8"
                 Text="{Binding TS, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Botones -->
        <StackPanel Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Aceptar"
                    Width="80"
                    Margin="0,8,8,0"
                    IsDefault="True"
                    Click="Aceptar_Click"/>
            <Button Content="Cancelar"
                    Width="80"
                    Margin="0,8,0,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
